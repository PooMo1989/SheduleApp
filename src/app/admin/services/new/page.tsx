'use client';

import { ServiceCreateForm } from '@/features/service/components';

/**
 * Create New Service Page
 * Story 2.3.1: Simple service creation form
 * After creation, redirects to full edit portal
 */
export default function NewServicePage() {
    return <ServiceCreateForm />;
}
