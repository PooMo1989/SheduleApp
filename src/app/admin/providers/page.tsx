'use client';

import { ProviderList } from '@/features/admin/components';

/**
 * Admin Providers Page (Tier 7 - Story 2.8.5)
 *
 * Displays the list of all service providers with detail view using SplitView pattern.
 */
export default function AdminProvidersPage() {
    return <ProviderList />;
}
